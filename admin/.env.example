# Base URL for the Laravel API. In dev the Vite proxy forwards /api to :8000,
# so this stays "/api". In production set this to your API origin, e.g.
# https://api.aniata.com or a same-origin path like /api.
VITE_API_URL=/api
