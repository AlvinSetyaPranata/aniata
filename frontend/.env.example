# Base URL of the Laravel backend API.
# The frontend calls this over HTTP using fetch().
# Dev: the Laravel dev server on :8000 (see AGENTS.md).
# Prod: set this to the hosted backend's /api URL.
VITE_API_URL=http://localhost:8000/api
